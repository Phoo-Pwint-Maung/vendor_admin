import 'package:flutter/material.dart';

// Default Color use in this application
class ColorConst {
  // final primaryColor = Color(0xFFD25F57);
  final primaryColor = Color(0xFF837066);
  final secondaryColor = Color(0xFF9C8A80);
  final white = Colors.white;
}

// FONT SIZE
class FontSizeConst {
  // Error Text Normal 12,
  final double firstFontSize = 12;
  // Paragraph Text
  // List Titles (medium weight)
  // Secondary text , Captions (Lighter color)
  // Buttons,Tabs (medium weight)
  final double secondFontSize = 14;
  // Text inputs
  final double thirdFontSize = 16;
  // Page Titles
  final double fourthFontSize = 20;
  final double biggestFontSize = 24;
}
