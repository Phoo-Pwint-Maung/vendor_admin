import 'package:flutter/material.dart';

class AddBarandScreen extends StatelessWidget {
  const AddBarandScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text("Add Brand Page"),
    );
  }
}
